export default function checkCollsion(trashList, crashDistance) {
    let distSqr = crashDistance * crashDistance;

    for (let i = 0; i < trashList.length; i++) {
        for (let j = i; j < trashList.length; j++) {
            let t1;

        }
    }
}